# 统一换行符为 LF。
# _headers / wrangler.jsonc 这些是给 Cloudflare 和工具读的，
# 仓库里存 CRLF 容易在 Linux CI 上出怪问题。
* text=auto eol=lf

# 二进制资源不要做换行转换
*.png binary
*.jpg binary
*.webp binary
*.ico binary
*.woff2 binary

# 明确标记语言，便于 GitHub 高亮
*.mjs text eol=lf
*.cjs text eol=lf
*.jsonc text eol=lf
_headers text eol=lf
